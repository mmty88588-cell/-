<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.UserMapper">

    <resultMap id="userMap" type="com.blog.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="avatar" column="avatar"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectById" resultMap="userMap">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="userMap">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.blog.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, email, avatar, role, status)
        VALUES (#{username}, #{password}, #{email}, #{avatar}, #{role}, #{status})
    </insert>

    <update id="updateById" parameterType="com.blog.entity.User">
        UPDATE `user`
        <set>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="password != null">password = #{password},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE `user` SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
